//! ACK generation utilities.
//!
//! The non-atomic `Bitmap` and word-level ACK range generation live in
//! [`bitmap`](crate::bitmap) and [`local`](crate::local) respectively.
//! `AckFrame` parsing lives in [`frame`](crate::frame).
